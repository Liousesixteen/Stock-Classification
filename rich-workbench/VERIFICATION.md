# Rich System V1.1 · 最终验收记录

## V1.1 股票优先与资金流修复（2026-08-23）

- 默认视图改为股票，顶部可切换基金；基金持仓、官方净值和披露穿透完整保留。
- 用户行情截图中的 600206、600460、603823 仅作为股票观察列表，明确标注“非持仓”，未生成数量、成本或个性化动作。
- 资金流占位模块已替换为真实数据：行业主力净流入/流出金额与占比、三只观察股票最新累计资金流。
- 修正累计口径：分钟资金流只取最后一个有效累计快照，不再跨分钟求和。
- 资金流均展示来源、单位、数据时点和模型免责声明；源失败时显示暂不可用，不用涨跌幅代理。
- 自动化测试增至 32/32 通过；真实浏览器验证股票/基金切换、1280 与 390 视口均无横向溢出。

验收时间：2026-08-23（Asia/Shanghai）

## 九阶段状态

1. 只读环境审计：通过。项目落在独立目录，未修改既有 `prototype/`、`cache/` 等文件。
2. 三个开场问题与范围锁定：通过。采用基金持仓穿透模式，保留全部功能，支持本机与手机访问。
3. 数据契约与私有持仓：通过。只录入用户确认的两项基金；截图未显示持仓日期，因此个性化动作保持锁定。
4. 数据采集与缓存：通过。行情、K 线、分时、官方净值、公开披露、真实 PE、新闻均具来源、时间、超时、缓存和降级状态。
5. 组合与穿透：通过。QDII 不用代理行情冒充官方结算；未披露部分不按零涨跌处理。
6. 信号与提醒：通过。M60、RSI、MACD 和估值阈值为条件状态，不生成确定买卖指令；提醒状态持久化并去重。
7. 前端与交互：通过。12 个规定区块、1440/1280/390 响应式、无横向溢出、移动菜单、蜡烛图、MA5/10/20/60、MACD、十字光标和日/周/月弹层均实现。
8. 安全与运行：通过。本机回环免登录；非回环必须 Basic Auth，且默认隐藏绝对金额；API 不进入 Service Worker 缓存。
9. 发布与回退：通过。候选和稳定快照只增不改，含 SHA256 清单，排除私有持仓、截图、状态与运行缓存。

## 自动化与真实运行结果

- Python/API/安全/前端契约测试：28/28 通过。
- JavaScript 语法与 Python 编译：通过。
- `/api/health`：200；`/api/ready`：200、ready=true。
- 真实首包：市场标的 14、行业 100、基金 2、新闻 30、技术信号 3、估值 3。
- 分时接口：沪深300 返回 242 个真实数据点。
- 基金穿透：012922 当前可解析披露覆盖 15.3%；016452 当前来源不可可靠解析，明确显示暂不可用、覆盖 0%、未覆盖 100%。
- 浏览器：1280 复验 12 区块、2 条持仓、无错误栏、无横向溢出；1440、1280、390 三档已验收，390 移动菜单最小高度 44px。

## 尚需用户提供的外部配置

手机访问认证代码已经完成，但没有替用户生成或保存密码。设置 `RICH_USER` 与 `RICH_PASSWORD` 后重启服务，手机才可通过可信局域网访问。公网访问还需要用户自有的 HTTPS 隧道或反向代理；当前没有声称已公网部署。

本工作台用于信息整理与条件观察，不构成投资建议，不替用户下单。
